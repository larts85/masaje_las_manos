name: 'coverage'

on:
  pull_request:
    branches:
      - master
      - develop

jobs:
  coverage:
    runs-on: ubuntu-latest
    needs: pipeline
    steps:
      - uses: actions/checkout@v3
      - uses: ArtiomTr/jest-coverage-report-action@v2